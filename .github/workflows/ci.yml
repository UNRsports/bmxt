name: CI

on:
  push:
    branches: [main, master]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      # Reproducible installs: exact tree from package-lock.json (never npm install in CI).
      - name: Install dependencies
        run: npm ci

      - name: Audit dependencies (fail on critical severity)
        run: npm audit --audit-level=critical

      - name: Verify manifest vs cmd CMD blocks
        run: npm run verify:manifest

      - name: No fetch() in extension pages / SW sources
        run: npm run check:no-fetch

      - name: Generated TS matches manifest (codegen + git diff)
        run: npm run check:generated

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Test
        run: npm test

      - name: Build extension
        run: npm run build
