{
  "0.1.5": {
    "ja": [
      "第二・第三コマンドの語彙を manifest の subcommands に集約し、codegen で command-subcommands.gen.ts と Rust の is_second_token を生成",
      "第一トークンのみ Enter 時の continuation と第二トークン Tab 補完を生成物に揃え、verify-manifest で各 head が cmd/*.rs にリテラルで存在するか検証",
      "README / .cursorrules に subcommands の追記手順（チェックリスト）を明記",
      "`grep` コマンド（-history / -bookmark / -page）でメモリ内検索。共有ロジックは lib/features/search、取得は grep-sources に分離",
      "`dom -list [--html|--react] [<pattern>]` で DOM を `grep -list` と同じピッカーに表示。第二コマンドは `-list` のみ（-show / -select / -hide はいったん廃止）。`--html` がデフォルト、`--react` も第三トークンのプルダウンで選択可能。pattern は部分一致の行フィルタ",
      "picker 直結経路（`grep -list` / `dom -list`）の対象タブ解決を background `resolveTabArg(undefined)` と同じロジックに揃える共有ユーティリティ `page-dom/resolve-target-tab.ts` を追加。これまで `dom -list --html` で常に「no target tab」になっていた回帰を修正",
      "`dom -list` 実行時に `executeScript` が失敗（保護ページ / オプション host 権限未許可 / 対象タブ無し等）した場合、picker 内に許可 (Enter / Y) / 拒否 (Esc / N) の確認パネルを表示。許可で `chrome.permissions.request` を user gesture 経由で発行して再試行、拒否で picker を終了",
      "`dom -list` の対象タブが Chrome Web Store (`chrome.google.com` / `chromewebstore.google.com`) や `chrome://` 系で **拡張機能スクリプトが Chrome により禁止されている** 場合を事前に URL から判定し、専用エラーで案内（共有 util: `lib/features/url/is-scriptable-page-url.ts`）",
      "`dom -list` ピッカー内の DOM テキストをマウスでドラッグ選択可能に。行内でのドラッグ開始時はキー入力 textarea にフォーカスを奪わず、選択が空のクリックでフォーカスを戻す。`↑↓ j/k Esc` 等のキー操作と両立",
      "`dom -list` の UI 表示からタブ ID を完全に除去。見出しはタイトルと URL のみ（タブ ID は明示的に要求があった場合以外は一切 UI に出さない方針）",
      "`dom -list` の flavor (`--html` / `--react`) 選択を第二コマンドと同じプルダウン UI で行うフローに変更。`dom -list` のみで Enter したら flavor プルダウンを表示し、`dom` → `-list` 選択時はそのまま flavor プルダウンへ自動連鎖（2 ステップで `dom -list --html` まで構成可能）",
      "`grep` の第二トークンを `is_second_token` と同じ ASCII 折りたたみ＋不可視文字除去で正規化し、`-page` 等で日本語パターン時の WASM 不整合を防止",
      "`dispatch::try_url_line` の末尾判定をバイトスライスから `strip_suffix` に変更（日本語入力行で UTF-8 境界パニック→WASM unreachable になっていた）"
    ],
    "en": [
      "Second/third command tokens live in manifest `subcommands`; codegen emits `command-subcommands.gen.ts` and Rust `is_second_token`",
      "Lone-first-token continuation and second-token Tab completion read from generated code; `verify:manifest` checks each `head` appears as a literal in `cmd/*.rs`",
      "README and `.cursorrules` document the `subcommands` add checklist",
      "`grep` command (`-history` / `-bookmark` / `-page`) searches in memory only; shared logic under `lib/features/search`, data adapters under `grep-sources`",
      "`dom -list [--html|--react] [<pattern>]` opens the DOM in the same picker chrome as `grep -list`; the only second token is `-list` (the previous `-show` / `-select` / `-hide` are dropped for now). The flavor pull-down is `--html` (default) / `--react`, and `<pattern>` is a case-insensitive substring line filter.",
      "New shared `page-dom/resolve-target-tab.ts` resolves the target tab for picker-direct dispatch (`grep -list` / `dom -list`) the same way as background `resolveTabArg(undefined)`; fixes a regression where `dom -list --html` always reported \"no target tab\"",
      "When `dom -list` fails (protected page, optional host permission missing, no target tab, …) the picker shows an approve (Enter / Y) / decline (Esc / N) panel. Approve fires `chrome.permissions.request` inside the user gesture and re-dispatches the same command line; decline closes the picker.",
      "`dom -list` detects up-front the target tabs Chrome forbids extension scripting on (Chrome Web Store `chrome.google.com` / `chromewebstore.google.com` and non-http(s) schemes such as `chrome://`, `chrome-extension://`, `about:`, `view-source:`, `data:`, `file:`, `devtools:`) via the new shared `is-scriptable-page-url.ts`, and surfaces a dedicated error instead of letting Chrome throw \"… cannot be scripted.\"",
      "DOM text inside the `dom -list` picker is now drag-selectable with the mouse: row clicks no longer steal focus from the key-input textarea, and focus is restored on mouseup when no selection was made. `↑↓ j/k Esc` keyboard controls keep working.",
      "Removed the chrome tab id from the `dom -list` UI entirely — the header now shows the tab title and URL only (tab ids stay out of UI until the user explicitly asks for them).",
      "`dom -list` flavor selection (`--html` / `--react`) now uses the same pull-down UI as the second-command picker: typing `dom -list` then Enter opens the flavor pull-down, and picking `-list` from the second-command pull-down auto-chains directly into the flavor pull-down (two steps from `dom` to `dom -list --html`).",
      "`grep` second token is normalized the same way as `is_second_token` (ASCII fold + strip invisible format chars) so `-page` with Japanese patterns cannot diverge and trap WASM",
      "`dispatch::try_url_line` suffix checks use `strip_suffix` instead of byte slicing (UTF-8 mid-char panic → WASM `unreachable` when the line contained Japanese)"
    ]
  }
}
