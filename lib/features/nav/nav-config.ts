/**
 * EN: Placeholder nav cursor movement (user-configurable later).
 * JA: 仮の移動量（将来ユーザー設定予定）。
 */

/** EN: Horizontal / vertical step in CSS pixels per arrow key. */
export const NAV_ARROW_STEP_PX = 12

/** EN: Cursor visual scale vs typical OS default (~16px); spec: 10% larger. */
export const NAV_CURSOR_SCALE = 1.1

/** EN: Keep nav cursor this many px inside the viewport when auto-scrolling. */
export const NAV_SCROLL_EDGE_MARGIN_PX = 32
